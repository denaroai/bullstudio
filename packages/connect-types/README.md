# @bullstudio/connect-types

Shared TypeScript contracts for Bullstudio queue adapters.

Normal embedded-mode users usually install a framework adapter and a queue
adapter instead of installing this package directly. Use this package when
building an advanced or custom queue adapter that needs to satisfy Bullstudio's
queue adapter contract.
